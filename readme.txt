ReadMe

1. To avoid possible compatibility issues please run with Java 11 or greater. 

2. Go to the project's root folder


3. build with "mvn package"

4. To run tests, run "mvn test"

Test with Sample files  (use --ftr flag to indicate the input txt file)
5. java -jar ./target/challenge-0.0.1-SNAPSHOT.jar --ftr="/path/to/file"

Note. I've included a couple of test files under "src/main/resources"